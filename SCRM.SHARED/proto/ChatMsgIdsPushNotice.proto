syntax = "proto3";

package  SCRM.SHARED.Proto;

// 聊天消息ID推送通知消息
message ChatMsgIdsPushNoticeMessage {
  string WeChatId = 1;   // 微信ID
  int64 StartTime = 2;   // 开始时间
  int64 EndTime = 3;     // 结束时间
  repeated int64 Ids = 4; // 消息ID列表
}
